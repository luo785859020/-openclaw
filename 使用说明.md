# OpenClaw + 小智 MCP 集成使用指南

## 概述
通过小智 AI 助手调用 OpenClaw，实现远程控制电脑、问答等功能。

---

## 一、OpenClaw 服务端配置

### 1. 启用 Webhook
在 `~/.openclaw/openclaw.json` 添加：

```json
{
  "hooks": {
    "enabled": true,
    "token": "openclaw123",
    "path": "/hooks"
  },
  "gateway": {
    "controlUi": {
      "allowedOrigins": ["*"],
      "allowInsecureAuth": true
    }
  }
}
```

### 2. 重启 Gateway
```bash
openclaw gateway restart
```

---

## 二、小智 MCP 配置

### 1. 文件结构
```
联网查询python代码/
├── 启动器.py          # 一键启动（图形界面）
├── openclaw_mcp.py    # MCP 核心程序
├── mcp_pipe.py        # WebSocket 转发
├── config_manager.py  # 配置管理
└── requirements.txt   # 依赖
```

### 2. 启动方式
双击 `启动器.py`，填写配置后点击「启动服务」

### 3. 配置参数

| 参数 | 说明 | 示例 |
|------|------|------|
| 小智MCP接入点 | 小智服务器地址 | `wss://api.xiaozhi.me/mcp/?token=xxx` |
| OpenClaw 地址 | OpenClaw 服务地址 | `http://localhost:18789` |
| Webhook Token | 验证密码 | `openclaw123` |

---

## 三、使用方法

在小智里直接说话，AI 会自动调用 MCP 工具把消息发给 OpenClaw


---

## 四、可用的功能

- 问答咨询
- 执行系统命令
- 文件操作
- 远程控制电脑
- 等等...

---

## 五、注意事项

1. **安全问题**：OpenClaw 会拒绝通过 webhook 执行系统命令，这是正常的防护机制
2. **飞书直连**：如果需要执行命令，建议直接通过飞书与 OpenClaw 对话
3. **网络要求**：小智服务器需要能访问到 OpenClaw 地址

---

## 六、故障排查

| 问题 | 解决方法 |
|------|----------|
| 连接失败 | 检查 OpenClaw 地址和 Token 是否正确 |
| 命令被拒绝 | 这是安全限制，建议通过飞书直接发命令 |
| MCP 无法启动 | 检查 Python 依赖是否安装：`pip install fastmcp requests` |

